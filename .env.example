# Copy to .env and adjust if needed. Docker Compose already sets these for
# inter-container use — this file is for running core/ locally outside Compose.
DATABASE_URL=postgresql://ulpf:ulpf@localhost:5432/ulpf
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=qwen2.5:3b
ULPF_RAW_DIR=data/raw
